# Регрессия после изменений синхронизации

- [ ] Прогнать каждый `ACTION` из [inventory.md](inventory.md) на dev/stage (минимум один раз перед prod).
- [ ] Проверить вход с секретом: задать `config.local.php` → `inbound_secret`, передать `X-Sync-Token` / `sync_token` с CRM.
- [ ] События пользователя: регистрация, обновление, удаление (цепочки `local/events/events.php`).
- [ ] Сценарий B24 → сайт: `UPDATE_MANAGER` (или актуальный smoke).
- [ ] Ответственный за прогон: *(указать в команде)*.
