# 05 — События и обратная совместимость

## Цель

`SyncEventHandlers` и вызовы `RegisterUserCompany` из событий `main` переключены на новый модуль; для внешних интеграций сохранены `class_alias` или статические прокси на переходный период.

## Критерии

- [ ] Список всех внешних ссылок на `OnlineService\B24\RegisterUserCompany` обновлён или покрыт alias.
- [ ] Срок снятия deprecated зафиксирован в ADR.
