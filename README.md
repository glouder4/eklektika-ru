# Установка 1С-Битрикс через Docker

Данный проект использует официальное контейнерное окружение для 1С-Битрикс от вендора: [bitrix-tools/env-docker](https://github.com/bitrix-tools/env-docker).

## Требования

- Docker версии 20.10 или выше
- Docker Compose версии 1.29 или выше (или Docker Compose V2)

## Быстрый старт

### 1. Клонирование официального репозитория

```bash
git clone https://github.com/bitrix-tools/env-docker.git bitrix-docker
cd bitrix-docker
```

### 2. Настройка окружения

В репозитории используются специализированные файлы конфигурации `.env_*` для разных компонентов:

- `.env_sql` - настройки баз данных (MySQL/PostgreSQL)
- `.env_php` - настройки PHP
- `.env_ssl` - настройки SSL/HTTPS
- `.env_redis` - настройки Redis
- `.env_push` - настройки Push-сервера

**⚠️ ВАЖНО:** Перед первым запуском обязательно измените пароли в файле `.env_sql`:

1. Откройте файл `.env_sql`
2. Замените `CHANGE_MYSQL_ROOT_PASSWORD_HERE` на ваш пароль для MySQL

Для генерации безопасных паролей можно использовать команду:
```bash
docker container run --rm alpine:3.21 sh -c "(cat /dev/urandom | tr -dc A-Za-z0-9\?\!\@\-\_\+\%\(\)\{\}\[\]\= | head -c 16) | tr -d '\' | tr -d '^' && echo ''"
```

При необходимости можно также настроить:
- Часовой пояс (создайте файл `.env` с параметром `TZ=Europe/Moscow`)
- Другие параметры в соответствующих файлах `.env_*`

### 3. Запуск контейнеров

```bash
docker-compose up -d
```

### 4. Проверка статуса

```bash
docker-compose ps
```

### 5. Доступ к сервисам

После успешного запуска контейнеров система будет доступна по следующим адресам:

- **Веб-интерфейс (HTTP):** http://localhost:8588 или http://127.0.0.1:8588
- **Веб-интерфейс (HTTPS):** https://localhost:8589 или https://127.0.0.1:8589

⚠️ **Важно:** Порты `8588` (HTTP) и `8589` (HTTPS) заданы в файле `docker-compose.yml`. При необходимости их можно изменить, отредактировав секцию `ports` сервиса `nginx` в `docker-compose.yml`.

> [!IMPORTANT]
> **НЕ** используйте `127.0.0.1` или `localhost` при работе с сайтом на локальной машине. Используйте IP адрес вашей локальной сети (например, `10.0.1.119`) или локальный домен (например, `dev.bx`).

## Базовая проверка конфигурации окружения

После запуска контейнеров рекомендуется провести базовую проверку конфигурации веб-сервера с помощью скрипта `bitrix_server_test.php`.

**Шаг 1:** Зайдите в консоль контейнера PHP:
```bash
docker compose exec --user=bitrix php sh
```

**Шаг 2:** Перейдите в корневой каталог сайта и скачайте скрипт проверки:
```bash
cd /opt/www/
wget https://dev.1c-bitrix.ru/download/scripts/bitrix_server_test.php
exit
```

**Шаг 3:** Откройте в браузере (замените IP на ваш):
```
http://10.0.1.119:8588/bitrix_server_test.php
```
или
```
http://localhost:8588/bitrix_server_test.php
```

> [!CAUTION]
> ⚠️ **Внимание!** После проверки конфигурации окружения скрипт `bitrix_server_test.php` нужно удалить.

**Расположение каталога сайта на хосте:**
Каталог сайта находится в папке проекта:
```
bitrix-docker/www/
```

Файлы монтируются в контейнеры через bind mount `./www:/opt/www/`, поэтому все изменения в папке `www/` на хосте сразу видны в контейнерах.

**Права доступа:**
Файлы и папки в каталоге `www/` должны принадлежать пользователю и группе `www:www`. Для настройки прав выполните:
```bash
cd bitrix-docker
sudo chown -R www:www www/
```

## Установка 1С-Битрикс

### Способ 1: Установка через bitrixsetup.php

**Шаг 1:** Зайдите в консоль контейнера PHP:
```bash
docker compose exec --user=bitrix php sh
```

**Шаг 2:** Перейдите в корневой каталог сайта и скачайте установщик:
```bash
cd /opt/www/
wget https://www.1c-bitrix.ru/download/scripts/bitrixsetup.php
exit
```

**Шаг 3:** Откройте в браузере (замените IP на ваш):
```
http://10.0.1.119:8588/bitrixsetup.php
```

**Шаг 4:** Следуйте инструкциям установщика. На шаге создания базы данных введите параметры подключения:

**Для MySQL/Percona:**
- **Имя хоста (алиас):** `percona` (или `mysql`, в зависимости от сервиса в `docker-compose.yml`)
- **Имя суперпользователя:** `root`
- **Пароль суперпользователя:** значение из файла `.env_sql` (параметр `MYSQL_ROOT_PASSWORD`)

**Для PostgreSQL:**
- **Имя хоста (алиас):** `postgres`
- **Имя суперпользователя:** `postgres`
- **Пароль суперпользователя:** значение из файла `.env_sql` (параметр `POSTGRES_PASSWORD`)

**Шаг 5:** После установки перейдите на сайт:
```
http://10.0.1.119:8588/
```

Проходите мастер установки дистрибутива:
- `1С-Битрикс: Управление сайтом` - [Документация](https://dev.1c-bitrix.ru/learning/course/index.php?COURSE_ID=135&CHAPTER_ID=04522&LESSON_PATH=10495.4495.4522)
- `1С-Битрикс24` - [Документация](https://dev.1c-bitrix.ru/learning/course/index.php?COURSE_ID=135&CHAPTER_ID=04522&LESSON_PATH=10495.4495.4522)

### Способ 2: Восстановление из резервной копии

Для восстановления сайта из резервной копии используйте скрипт `restore.php`:

**Шаг 1:** Зайдите в консоль контейнера PHP:
```bash
docker compose exec --user=bitrix php sh
```

**Шаг 2:** Скачайте скрипт восстановления:
```bash
cd /opt/www/
wget https://www.1c-bitrix.ru/download/scripts/restore.php
exit
```

**Шаг 3:** Откройте в браузере:
```
http://10.0.1.119:8588/restore.php
```

**Варианты восстановления:**
- Скачать резервную копию с другого сайта
- Развернуть резервную копию из облака "1С-Битрикс" (требуется лицензионный ключ)
- Загрузить с локального диска

После завершения распаковки укажите настройки соединения с базой данных (если при создании резервной копии был создан дамп БД).

> [!CAUTION]
> ⚠️ **Внимание!** После успешного восстановления нажмите кнопку "Удалить локальную копию и служебные скрипты".

## Установка phpMyAdmin

phpMyAdmin — это веб-интерфейс для управления базами данных MySQL/Percona. Он позволяет удобно работать с базой данных через браузер.

> [!NOTE]
> **Важно:** В оригинальном репозитории `bitrix-tools/env-docker` секция `phpmyadmin` отсутствует в `docker-compose.yml`. Необходимо добавить её вручную.

### Шаг 1: Добавление секции phpmyadmin в docker-compose.yml

Откройте файл `docker-compose.yml` и добавьте секцию `phpmyadmin` перед секцией `networks:` (в конце файла, перед определением сетей).

Добавьте следующую конфигурацию:

```yaml
  phpmyadmin:
    image: phpmyadmin/phpmyadmin:latest
    container_name: dev_phpmyadmin
    restart: unless-stopped
    env_file:
      - .env_sql
    environment:
      PMA_ARBITRARY: 1
      PMA_HOST: mysql
      PMA_PORT: 3306
      UPLOAD_LIMIT: 300M
      MAX_EXECUTION_TIME: 600
    ports:
      - "8080:80"
    networks:
      dev:
        aliases:
          - phpmyadmin
    depends_on:
      - mysql
```

**Важно:** 
- Убедитесь, что отступы соответствуют другим сервисам в файле (обычно 2 пробела)
- Секция должна быть добавлена перед секцией `networks:` в конце файла
- **Определение имени сервиса БД:** Откройте `docker-compose.yml` и найдите секцию с базой данных (обычно начинается с `mysql:` или `percona:`). Имя этой секции (без двоеточия) нужно использовать в двух местах:
  - В параметре `PMA_HOST:` (например, `PMA_HOST: mysql` или `PMA_HOST: percona`)
  - В секции `depends_on:` (например, `- mysql` или `- percona`)

### Шаг 2: Настройка порта (опционально)

По умолчанию phpMyAdmin будет доступен на порту `8080`. Если этот порт занят, измените значение в секции `ports`:

```yaml
ports:
  - "8081:80"  # вместо 8080:80
```

### Шаг 3: Запуск контейнера phpMyAdmin

Запустите контейнер phpMyAdmin:

```bash
docker-compose up -d phpmyadmin
```

Или перезапустите все контейнеры:

```bash
docker-compose up -d
```

### Шаг 4: Проверка статуса

Убедитесь, что контейнер запущен:

```bash
docker-compose ps phpmyadmin
```

### Шаг 5: Доступ к phpMyAdmin

Откройте в браузере (замените IP на ваш):

```
http://10.0.1.119:8080
```

или

```
http://localhost:8080
```

### Шаг 6: Вход в phpMyAdmin

Для входа в phpMyAdmin используйте параметры подключения к базе данных из файла `.env_sql`:

**Для MySQL/Percona:**
- **Сервер:** имя сервиса из `docker-compose.yml` (обычно `mysql` или `percona`) — должно совпадать с тем, что указано в параметре `PMA_HOST` в конфигурации phpMyAdmin
- **Имя пользователя:** `root`
- **Пароль:** значение из файла `.env_sql` (параметр `MYSQL_ROOT_PASSWORD`)

**Примечание:** Если в конфигурации phpMyAdmin указан `PMA_HOST: mysql`, то при входе в веб-интерфейсе в поле "Сервер" также нужно указать `mysql`. Если указан `PMA_HOST: percona`, то соответственно `percona`.

### Параметры конфигурации

В конфигурации phpMyAdmin используются следующие параметры:

- `PMA_ARBITRARY: 1` — позволяет подключаться к любому серверу MySQL
- `PMA_HOST: mysql` — имя хоста базы данных (должно соответствовать имени сервиса в `docker-compose.yml`)
- `PMA_PORT: 3306` — порт MySQL
- `UPLOAD_LIMIT: 300M` — максимальный размер загружаемого файла
- `MAX_EXECUTION_TIME: 600` — максимальное время выполнения скрипта (в секундах)

### Решение проблем

**Порт 8080 уже занят:**
Измените порт в файле `docker-compose.yml` в секции `ports` сервиса `phpmyadmin` и перезапустите контейнер:

```bash
docker-compose up -d phpmyadmin
```

**Ошибка подключения к базе данных:**
- Убедитесь, что контейнер MySQL/Percona запущен: `docker-compose ps mysql`
- Проверьте, что имя хоста в phpMyAdmin соответствует имени сервиса в `docker-compose.yml` (обычно `mysql` или `percona`)
- Убедитесь, что параметры подключения в `.env_sql` корректны

**Контейнер не запускается:**
Проверьте логи контейнера:

```bash
docker-compose logs phpmyadmin
```

## Управление контейнерами

**Остановить контейнеры:**
```bash
docker-compose stop
```

**Запустить контейнеры:**
```bash
docker-compose start
```

**Остановить и удалить контейнеры:**
```bash
docker-compose down
```

**Остановить и удалить контейнеры с данными БД:**
```bash
docker-compose down -v
```

**Просмотр логов:**
```bash
docker-compose logs -f
```

**Просмотр логов конкретного сервиса:**
```bash
docker-compose logs -f php
docker-compose logs -f mysql
```

## Структура сервисов

Официальное окружение включает следующие сервисы (могут отличаться в зависимости от версии):

- **nginx** - Nginx веб-сервер (официальный образ `bitrix24/nginx`)
- **php** - PHP-FPM (официальный образ `bitrix24/php`)
- **percona** или **mysql** - База данных Percona Server или MySQL (официальный образ `bitrix24/percona-server`)
- **phpmyadmin** - phpMyAdmin для управления БД (опционально)

Все образы являются официальными и поддерживаются вендором 1С-Битрикс.

## Порты

Порты заданы напрямую в файле `docker-compose.yml`. По умолчанию используются:
- **8588** - HTTP (Nginx)
- **8589** - HTTPS (Nginx)
- **8080** - phpMyAdmin (если установлен)

Остальные сервисы (MySQL/Percona, Redis, Memcached и т.д.) работают внутри Docker-сети и не имеют публичных портов по умолчанию (порты закомментированы в `docker-compose.yml`).

⚠️ **Важно:** Если нужно изменить порты веб-сервера, отредактируйте секцию `ports` сервиса `nginx` в файле `docker-compose.yml`. Для изменения порта phpMyAdmin отредактируйте секцию `ports` сервиса `phpmyadmin`.

## Важные замечания

⚠️ **Безопасность:** Перед использованием в продакшене обязательно измените пароли в файле `.env_sql`!

📁 **Данные:** Все файлы Битрикс размещаются в директории `www/` проекта (bind mount). Данные БД сохраняются в Docker volumes. Файлы должны иметь права доступа `www:www`.

🔧 **Настройки:** Конфигурационные файлы находятся в директории `confs/` репозитория. При необходимости можно настроить Nginx, PHP и другие сервисы.

📚 **Документация:** Подробная документация и актуальные инструкции доступны в официальном репозитории: [https://github.com/bitrix-tools/env-docker](https://github.com/bitrix-tools/env-docker)

🔄 **Обновления:** Рекомендуется регулярно проверять обновления официального репозитория для обеспечения безопасности и стабильности системы.

## Решение проблем

**Порт уже занят:**
Измените порты в файле `docker-compose.yml` в секции `ports` сервиса `nginx`. Например:
```yaml
ports:
  - "8080:80"    # вместо 8588:80
  - "8443:443"   # вместо 8589:443
```

**Ошибки подключения к БД:**
- Убедитесь, что используете имя сервиса из `docker-compose.yml` как хост БД (обычно `percona` для Percona Server или `postgres` для PostgreSQL), а не `localhost`
- Проверьте правильность параметров подключения в файле `.env_sql`
- Убедитесь, что контейнер БД запущен: `docker-compose ps`

**Проблемы с правами доступа:**
В WSL2/Linux может потребоваться настроить права на папку `www/`. Файлы должны принадлежать пользователю и группе `www:www`:
```bash
cd bitrix-docker
sudo chown -R www:www www/
```

Если пользователь `www` не существует, создайте его:
```bash
sudo groupadd www
sudo useradd -g www www
sudo chown -R www:www www/
```

**Проблемы с портами:**
- Проверьте, не заняты ли порты другими приложениями: `netstat -tulpn | grep <порт>`
- Измените порты в файле `docker-compose.yml` в секции `ports` сервиса `nginx` и перезапустите контейнеры

**Ошибка 403 Forbidden:**
- Папка `www/` пуста. Скачайте установщик Битрикс (см. раздел "Установка 1С-Битрикс")
- Проверьте права доступа к файлам в папке `www/` (должны быть `www:www`)

**Дополнительная помощь:**
- Официальная документация: [https://github.com/bitrix-tools/env-docker](https://github.com/bitrix-tools/env-docker)
- Issues в репозитории: [https://github.com/bitrix-tools/env-docker/issues](https://github.com/bitrix-tools/env-docker/issues)
- Документация по установке: [https://dev.1c-bitrix.ru/learning/course/index.php?COURSE_ID=135&LESSON_ID=4523](https://dev.1c-bitrix.ru/learning/course/index.php?COURSE_ID=135&LESSON_ID=4523)

## Дополнительные возможности

Официальное окружение поддерживает множество дополнительных возможностей, подробно описанных в оригинальном README:

- **Cron** - выполнение агентов и кастомных заданий
- **Push & Pull** - настройка Push-сервера для интерактивности
- **Sphinx** - полнотекстовый поиск
- **Почта** - настройка отправки через msmtp или SMTP
- **HTTPS/SSL** - самоподписанные или Let's Encrypt сертификаты
- **Кеширование** - настройка Memcache и Redis
- **Хранение сессий** - настройка хранения сессий в БД, Redis или Memcache
- **PHP расширения** - активация/деактивация расширений
- **Nginx модули** - подключение дополнительных модулей

Подробные инструкции по всем этим возможностям доступны в [официальном README репозитория](https://github.com/bitrix-tools/env-docker/blob/main/README.md).

