# 🚀 Настройка CI/CD для автоматического деплоя

Этот файл содержит подробную инструкцию по настройке CI/CD для автоматического деплоя сайта на сервер.

## 📋 Предварительные требования

- Репозиторий на GitHub
- Тестовый сервер с SSH доступом (сейчас)
- Боевой сервер с SSH доступом (потом)
- Права на добавление секретов в репозиторий

## 🏗️ Структура деплоя

- **Тестовый сервер** (`deploy.yml`) - автоматический деплой при push в `main`
- **Боевой сервер** (`deploy-production.yml`) - ручной деплой через GitHub Actions (пока не настроен)

## 🔧 Настройка для GitHub Actions

### Шаг 1: Создание SSH ключа на тестовом сервере

```bash
# Подключитесь к тестовому серверу по SSH
ssh user@test-server.com

# Создайте SSH ключ для CI/CD
ssh-keygen -t ed25519 -C "github-actions-test-deploy" -f ~/.ssh/github_actions_test_deploy

# Добавьте публичный ключ в authorized_keys
cat ~/.ssh/github_actions_test_deploy.pub >> ~/.ssh/authorized_keys

# Установите правильные права
chmod 600 ~/.ssh/authorized_keys
chmod 600 ~/.ssh/github_actions_test_deploy
chmod 644 ~/.ssh/github_actions_test_deploy.pub

# Скопируйте приватный ключ (понадобится для следующего шага)
cat ~/.ssh/github_actions_test_deploy
```

### Шаг 2: Добавление секретов для тестового сервера в GitHub

1. Откройте ваш репозиторий на GitHub
2. Перейдите в **Settings** → **Secrets and variables** → **Actions**
3. Нажмите **New repository secret**
4. Добавьте следующие секреты для **тестового сервера**:

| Имя секрета | Описание | Пример значения |
|------------|----------|----------------|
| `TEST_SSH_PRIVATE_KEY` | Приватный SSH ключ тестового сервера | `-----BEGIN OPENSSH PRIVATE KEY-----...` |
| `TEST_SERVER_HOST` | IP адрес или домен тестового сервера | `test.example.com` или `192.168.1.100` |
| `TEST_SERVER_USER` | Пользователь для SSH подключения | **См. рекомендации ниже** |
| `TEST_DEPLOY_PATH` | Путь на тестовом сервере куда деплоить | `/var/www/html` |
| `TEST_WEB_USER` | Пользователь веб-сервера | `www-data` |
| `TEST_WEB_GROUP` | Группа веб-сервера | `www-data` |

#### 🤔 Какой пользователь выбрать для `TEST_SERVER_USER`?

**Вариант 1: `root` (проще для начала)**
- ✅ Не нужно настраивать права
- ✅ Работает сразу "из коробки"
- ❌ Менее безопасно (полный доступ к серверу)
- ✅ **Рекомендуется для тестового сервера**, если это ваш личный/внутренний сервер

**Вариант 2: `deploy` (безопаснее)**
- ✅ Более безопасно (ограниченные права)
- ✅ Лучшая практика для production
- ❌ Нужно создать пользователя и настроить права
- ✅ **Рекомендуется для боевого сервера**

**Рекомендация:**
- **Для тестового сервера:** используйте `root` (проще и быстрее настроить)
- **Для боевого сервера:** создайте отдельного пользователя `deploy` (безопаснее)

Если выберете `deploy`, выполните на сервере:
```bash
# Создать пользователя deploy
sudo useradd -m -s /bin/bash deploy

# Добавить его в группу веб-сервера (чтобы мог менять права файлов)
sudo usermod -aG www-data deploy

# Настроить sudo для chown (опционально, если нужно менять владельца файлов)
echo "deploy ALL=(ALL) NOPASSWD: /bin/chown" | sudo tee /etc/sudoers.d/deploy
```

**Важно:** 
- Для `TEST_SSH_PRIVATE_KEY` скопируйте весь ключ, включая строки `-----BEGIN` и `-----END`
- Не добавляйте переносы строк вручную, просто вставьте весь ключ

### Шаг 3: Проверка работы тестового деплоя

1. Сделайте изменения в `bitrix-docker/www/`
2. Закоммитьте и запушьте в ветку `main`:
```bash
git add bitrix-docker/www/
git commit -m "Test CI/CD deployment"
git push origin main
```

3. Перейдите в **Actions** в вашем репозитории
4. Увидите запущенный workflow "Deploy to Test Server"
5. Дождитесь завершения (зеленая галочка = успех)

### Шаг 4: Ручной запуск деплоя на тестовый сервер

Если нужно задеплоить без коммита:
1. Перейдите в **Actions**
2. Выберите workflow "Deploy to Test Server"
3. Нажмите **Run workflow**
4. Выберите ветку и нажмите **Run workflow**

## 🚀 Настройка боевого сервера (когда будет готов)

### Шаг 1: Создание SSH ключа на боевом сервере

```bash
# Подключитесь к боевому серверу
ssh user@production-server.com

# Создайте SSH ключ для production
ssh-keygen -t ed25519 -C "github-actions-production-deploy" -f ~/.ssh/github_actions_production_deploy
cat ~/.ssh/github_actions_production_deploy.pub >> ~/.ssh/authorized_keys
chmod 600 ~/.ssh/authorized_keys
chmod 600 ~/.ssh/github_actions_production_deploy

# Скопируйте приватный ключ
cat ~/.ssh/github_actions_production_deploy
```

### Шаг 2: Добавление секретов для боевого сервера

Добавьте следующие секреты в GitHub (Settings → Secrets):

| Имя секрета | Описание | Пример значения |
|------------|----------|----------------|
| `PRODUCTION_SSH_PRIVATE_KEY` | Приватный SSH ключ боевого сервера | `-----BEGIN OPENSSH PRIVATE KEY-----...` |
| `PRODUCTION_SERVER_HOST` | IP адрес или домен боевого сервера | `example.com` |
| `PRODUCTION_SERVER_USER` | Пользователь для SSH | `root` или `deploy` |
| `PRODUCTION_DEPLOY_PATH` | Путь на боевом сервере | `/var/www/html` |
| `PRODUCTION_WEB_USER` | Пользователь веб-сервера | `www-data` |
| `PRODUCTION_WEB_GROUP` | Группа веб-сервера | `www-data` |

### Шаг 3: Активация деплоя на боевой сервер

1. Откройте файл `.github/workflows/deploy-production.yml`
2. Раскомментируйте секцию `push` в `on:` (уберите `#` перед строками)
3. Закоммитьте изменения:
```bash
git add .github/workflows/deploy-production.yml
git commit -m "Enable production deployment"
git push origin main
```

Теперь деплой на боевой сервер будет запускаться автоматически при push в `main`.

**⚠️ ВАЖНО:** Боевой сервер настроен на ручной запуск через `workflow_dispatch` для безопасности. Вы всегда можете запустить деплой вручную через Actions → Deploy to Production → Run workflow.

## 🔍 Отладка проблем

### Проблема: "Permission denied (publickey)"

**Решение:**
- Убедитесь, что публичный ключ добавлен в `~/.ssh/authorized_keys` на сервере
- Проверьте права: `chmod 600 ~/.ssh/authorized_keys`
- Убедитесь, что приватный ключ скопирован полностью (включая BEGIN/END строки)

### Проблема: "Host key verification failed"

**Решение:**
- GitHub Actions автоматически добавляет хост в known_hosts через `ssh-keyscan`

### Проблема: "rsync: connection refused"

**Решение:**
- Убедитесь, что на сервере установлен rsync: `apt-get install rsync` (или `yum install rsync`)
- Проверьте, что SSH доступ работает: `ssh user@server`

### Проблема: "chown: invalid user"

**Решение:**
- Проверьте, что пользователь и группа веб-сервера существуют на сервере
- Обычно это `www-data:www-data` (Debian/Ubuntu) или `nginx:nginx` (CentOS)

### Проблема: "Secret not found"

**Решение:**
- Убедитесь, что все секреты добавлены с правильными именами (с префиксом `TEST_` для тестового сервера)
- Проверьте, что секреты добавлены в правильный репозиторий

## 📊 Мониторинг деплоев

### GitHub Actions
- Все деплои видны в разделе **Actions**
- Можно настроить уведомления в Settings → Notifications
- Каждый деплой показывает логи выполнения всех шагов

## 🔐 Безопасность

1. **Никогда не коммитьте секреты в репозиторий!**
2. Используйте отдельный SSH ключ только для CI/CD
3. Ограничьте права этого ключа (можно через `authorized_keys` с опциями)
4. Регулярно ротируйте ключи
5. Используйте разные ключи для production и staging

## 🎯 Дополнительные возможности

### Деплой только при изменении определенных файлов

В `.github/workflows/deploy.yml` уже настроено:
```yaml
paths:
  - 'bitrix-docker/www/**'
```

Это означает, что деплой запустится только если изменены файлы в `bitrix-docker/www/`.

### Уведомления в Telegram/Slack

Можно добавить шаг для отправки уведомлений:

```yaml
- name: Send Telegram notification
  uses: appleboy/telegram-action@master
  with:
    to: ${{ secrets.TELEGRAM_CHAT_ID }}
    token: ${{ secrets.TELEGRAM_BOT_TOKEN }}
    message: |
      ✅ Деплой завершен успешно!
      Коммит: ${{ github.sha }}
      Автор: ${{ github.actor }}
```

## 📝 Чеклист перед первым деплоем на тестовый сервер

- [ ] SSH ключ создан на тестовом сервере
- [ ] Публичный ключ добавлен в `authorized_keys`
- [ ] Все секреты с префиксом `TEST_` добавлены в GitHub
- [ ] Права на тестовом сервере настроены правильно
- [ ] Путь деплоя существует и доступен
- [ ] Тестовый деплой выполнен успешно
- [ ] Сайт работает после деплоя на тестовом сервере

## 📝 Чеклист для настройки боевого сервера (когда будет готов)

- [ ] SSH ключ создан на боевом сервере
- [ ] Публичный ключ добавлен в `authorized_keys`
- [ ] Все секреты с префиксом `PRODUCTION_` добавлены в GitHub
- [ ] Файл `deploy-production.yml` активирован (раскомментирован push)
- [ ] Тестовый деплой на боевой сервер выполнен успешно
- [ ] Сайт работает после деплоя на боевом сервере

---

**Готово!** Теперь каждый `git push` будет автоматически обновлять сайт на сервере! 🎉

